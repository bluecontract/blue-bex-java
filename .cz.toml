[tool.commitizen]
# The release workflow updates this version when it creates an RC tag.
name = "cz_conventional_commits"
tag_format = "v$version"
version_scheme = "semver"
version = "1.1.0-rc.5"
update_changelog_on_bump = true
