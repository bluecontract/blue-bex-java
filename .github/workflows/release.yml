name: Release
on:
  workflow_dispatch:
permissions:
  contents: read
  actions: read
concurrency:
  group: bex-release-stable
  cancel-in-progress: false
jobs:
  Release:
    uses: ./.github/workflows/release-verification.yml
    with:
      mode: stable
    secrets: inherit
